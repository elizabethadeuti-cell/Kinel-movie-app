

const Header = ({ label }) => {

  return(
    <header>
      <button>{label}</button>
      <button>{label}</button>
      <button>{label}</button>
      <button>{label}</button>

    </header>
  )
}

export default Header;